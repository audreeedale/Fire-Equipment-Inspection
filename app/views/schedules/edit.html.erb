<% content_for :title, "Edit Schedule" %>

<h1 class="text-2xl font-bold mb-6">Edit Schedule — <%= @schedule.address.display_name %></h1>
<%= render "form", schedule: @schedule, address: @schedule.address %>
